# area formula prism = x1 * y1


def prism_area(one, two):
    return one * two
print('Units count, so make sure they are consistent!')

side_one = float(input("x1: "))
side_two = float(input("y1: "))

print(prism_area(side_one, side_two), 'square units')
